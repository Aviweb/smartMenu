{
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../.. && npx prisma generate --schema=packages/db/prisma/schema.prisma && cd apps/web && next build",
  "installCommand": "npm install"
}
